/**
 * Battlefield background art — stretched to the map panel; grid (16×9) aligns to the same bounds.
 * Swap the require target if your design uses another asset (e.g. a full-map JPG).
 */
export const BATTLEFIELD_MAP_IMAGE = require('../assets/images/path.png');
